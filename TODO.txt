1) if a doubt gets resloved then send notification to all those who had made request for that doubt and remove their requests
2) if student delete a doubt then send notification to all debuggers who have requested and remove their requests
3) Show the notification of most rated debugger at top -> descreasing order of rating
4) Instead of storing userId and Some User Data in LocalStorage , Store it inside useCOntext()








DOUBTS/Future Problems ca be
1) useLocation().state -> what if i am not coming to that component using navigate method